# DisasterView Pipeline Dependencies
# Install with: pip install -r requirements.txt

# Core utilities
tqdm>=4.66
numpy>=1.24
Pillow>=10.0
opencv-python>=4.8

# Video download
yt-dlp>=2024.1.0

# Scene detection
scenedetect[opencv]>=0.6.3

# Deep learning (CPU build; replace with torch+cu121 for CUDA)
torch>=2.1.0
torchvision>=0.16.0

# CLIP (install via git)
# git+https://github.com/openai/CLIP.git

# Annotation utilities
supervision>=0.19

# Stage 4 auto-labeling: no local GPU models needed.
# Primary:  Roboflow cloud auto-label (set ROBOFLOW_API_KEY)
# Fallback: CLIP (installed above) + OpenCV GrabCut (opencv-python installed above)

# Roboflow
roboflow>=1.1.30

# Environment variables (.env file support)
python-dotenv>=1.0
